apiVersion: v1
kind: Service
metadata:
  name: hello-world-vjasusclxphk
  labels:
    cloud.sealos.io/app-deploy-manager: hello-world
spec:
  ports:
    - port: 80
      targetPort: 80
      name: pquueqjfyqai
      protocol: TCP
  selector:
    app: hello-world
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world
  annotations:
    originImageName: nginx
    deploy.cloud.sealos.io/minReplicas: '1'
    deploy.cloud.sealos.io/maxReplicas: '1'
    deploy.cloud.sealos.io/resize: '0'
  labels:
    cloud.sealos.io/app-deploy-manager: hello-world
    app: hello-world
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: hello-world
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: hello-world
        restartTime: '20260713172736'
    spec:
      automountServiceAccountToken: false
      containers:
        - name: hello-world
          image: nginx
          env: []
          resources:
            requests:
              cpu: 20m
              memory: 25Mi
            limits:
              cpu: 200m
              memory: 256Mi
          ports:
            - containerPort: 80
              name: pquueqjfyqai
          imagePullPolicy: Always
          volumeMounts: []
      volumes: []
